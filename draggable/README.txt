Notes about the project:
- Tested on:
    IE9 and iOS
    Latest versions of Chrome, Firefox, Safari
- Uses slider done previously to change the square's border radius
— The square is only draggable within the window bounds
- The square is dragged from point where it is clicked on desktop, and from the center in mobile
